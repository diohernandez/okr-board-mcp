{
  "name": "mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor MCP stdio: envuelve core, expone las tools de contracts/okr-board-mcp.tools.json.",
  "scripts": {
    "build": "tsc -b",
    "mcp": "node dist/src/server.js"
  },
  "dependencies": {
    "core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
