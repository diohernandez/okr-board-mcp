{
  "name": "core",
  "version": "0.1.0",
  "private": true,
  "description": "Pipeline puro de autoría del tablero OKR: tipos, validación en cascada, puertos SpecStore/MetricCatalog. Sin conocimiento de MCP ni HTTP.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b && node dist/test/pipeline.test.js && node dist/test/git-store.test.js && node dist/test/metric-catalog.test.js && node dist/test/file-lock.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
